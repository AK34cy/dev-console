#!/usr/bin/env bash
set -euo pipefail

repository_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

printf 'bin/install-dev-console is retired. Use sudo ./install.sh from the repository root.\n' >&2
exec "$repository_root/install.sh" "$@"
